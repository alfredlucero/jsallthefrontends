---
// Full Astro Component Syntax:
// https://docs.astro.build/core-concepts/astro-components/
// We need to wrap the AlpineJS usage within an Astro component so we can pass props to initialize Alpine with a certain state
export interface HelloWorldProps {
  name?: string;
}
const { name = "World" } = Astro.props;
---

<p>Hello {name}!</p>
